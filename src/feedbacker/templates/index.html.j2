{% extends "base.html.j2" %}
{% block title %}Index{% endblock %}
{% block head %}
    {{ super() }}
    <style type="text/css">
        .important { color: #336699; }
    </style>
{% endblock %}
{% block content %}
    <h1>My Courses</h1>

    <div class="list-group mt-3">
      {% for course in courses %}
      <a href="{{ url_for('course_home', course_id=course.id) }}" class="list-group-item list-group-item-action flex-column align-items-start">
        <div class="d-flex w-100 justify-content-between">
          <h5 class="mb-1">{{ course.name }}</h5>
          {# <small>3 days ago</small> #}
        </div>
        <p class="mb-1">{{ course.code }}</p>
        <small>{% for instructor in course.instructors %}
                {{ instructor.firstname }} {{ instructor.lastname }}
              {% endfor %}
        </small>
      </a>
      {% endfor %}
    </div>

    {# {% for course in courses %}
    <a href="#" class="text-decoration-none">
      <div class="card border-primary mb-3">
        <div class="card-header">{{ course.code }}</div>
        <div class="card-body">
          <h4 class="card-title">{{ course.name }}</h4>
          <p class="card-text">
            {% for instructor in course.instructors %}
              {{ instructor.firstname }} {{ instructor.lastname }}
            {% endfor %}
          </p>
        </div>
      </div>
    </a>
    {% endfor %} #}

    <button type="button" class="btn btn-primary mt-3">Refresh Course List</button>

    <!-- <form data-bitwarden-watching="1">
      <fieldset>
        <div>
          <label for="exampleInputEmail1" class="form-label mt-4">Email address</label>
          <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Enter email">
          <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
        </div>
        <div>
          <label for="exampleInputPassword1" class="form-label mt-4">Password</label>
          <input type="password" class="form-control" id="exampleInputPassword1" placeholder="Password" autocomplete="off">
        </div>
        <div>
          <label for="exampleTextarea" class="form-label mt-4">Example textarea</label>
          <textarea class="form-control" id="exampleTextarea" rows="3"></textarea>
        </div>
        <div>
          <label for="formFile" class="form-label mt-4">Default file input example</label>
          <input class="form-control" type="file" id="formFile">
        </div>
      </fieldset>
    </form> -->
{% endblock %}